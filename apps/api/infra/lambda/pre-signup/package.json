{
  "name": "pre-signup-lambda",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild index.ts --bundle --platform=node --target=node20 --outfile=dist/index.js --external:@aws-sdk/*",
    "package": "powershell -Command \"Compress-Archive -Path dist/index.js -DestinationPath pre-signup.zip -Force\""
  },
  "devDependencies": {
    "@aws-sdk/client-cognito-identity-provider": "^3.600.0",
    "esbuild": "^0.21.0"
  }
}
